import React, { Fragment } from 'react';
import AddContact from '../../components/AddContact/AddContact';

const Add = () => {
  return (
    <Fragment>
      <AddContact/>
    </Fragment>
  );
};

export default Add;